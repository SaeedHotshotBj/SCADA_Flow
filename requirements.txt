# SCADA_FLOW - Python dependencies
# Install: pip install -r requirements.txt

Flask>=3.0.0,<4.0.0
Flask-SocketIO>=5.3.0,<6.0.0

# Jalali (Persian) date support
jdatetime>=4.1.0,<7.0.0

# Modbus TCP (PLC read/write) — v2.x required for pymodbus.client.sync imports
pymodbus>=2.5.3,<3.0.0
